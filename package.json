{
  "name": "fireats",
  "version": "0.0.0",
  "description": "Finished code for the Firestore codelab",
  "main": ".",
  "scripts": {
    "firebase:emulators": "firebase emulators:start --only firestore",
    "test": "jest",
    "fmt": "prettier --write scripts/* styles/*"
  },
  "author": "",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.2",
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "prettier": "^1.5.3",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "firebase": "^9.6.8"
  }
}
